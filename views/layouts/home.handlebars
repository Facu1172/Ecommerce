<!DOCTYPE html>
<html>
  <head>
    <title>E-commerce</title>
  </head>
  <body>
    <h1>Products</h1>
    <ul>
      {{#each products}}
      <li>{{this.name}}</li>
      {{/each}}
    </ul>
  </body>
</html>